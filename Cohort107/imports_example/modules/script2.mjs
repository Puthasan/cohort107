import { answer as supremeAnswer, addTwoNums } from "./script1.mjs";

let newAnswer = 0
function supremeAnswer(c) {
  newAnswer = answer + c
  console.log(newAnswer);
}

addTwoNums(1, 2)
addTwoMoreNums(3)